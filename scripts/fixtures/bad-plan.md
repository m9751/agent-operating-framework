# Bad Plan Fixture

> Used by `rules-lint.yml` to smoke-test that `validate-done-criteria.py` rejects prose-only Done Criteria. Do not delete.

## Done Criteria

1. The validator works correctly
2. Code is high quality
3. Tests pass
